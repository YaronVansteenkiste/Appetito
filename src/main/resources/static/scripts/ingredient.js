function addPersons() {
    const personsEl = document.getElementById('persons');
    personsEl.innerText = parseInt(personsEl.textContent)+1;
}

function subtractPersons() {
    const personsEl = document.getElementById('persons');
    personsEl.innerText = parseInt(personsEl.textContent) +1;
}